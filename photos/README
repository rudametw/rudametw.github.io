Add directory following naming convention.

Edit _config.yml to add best photo and gallery.

Add empty index.html into the gallery so the site is found.

Touch empty index file so that sitemap is properly updated.
